# Required for live Gemini diagnosis. Leave empty to use the labeled,
# deterministic fallback during offline development and tests.
GEMINI_API_KEY=

# Pin this only after confirming the exact Gemini 3.5+ model ID available to
# the contest account. The application currently has a development default.
# GEMINI_MODEL=
